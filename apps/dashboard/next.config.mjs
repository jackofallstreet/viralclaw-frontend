/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: [
    "@viralclawos/ui",
    "@viralclawos/utils",
    "@viralclawos/core",
    "@viralclawos/agents",
  ],
};

export default nextConfig;
