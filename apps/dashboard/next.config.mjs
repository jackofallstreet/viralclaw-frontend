/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: [
    "@viralclaw/ui",
    "@viralclaw/utils",
    "@viralclaw/core",
    "@viralclaw/agents",
  ],
};

export default nextConfig;
